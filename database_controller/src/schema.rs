table! {
    plugin_infos (id) {
        id -> Varchar,
        name -> Varchar,
        description -> Varchar,
        thumbnail_url -> Varchar,
        author -> Varchar,
        tags -> Varchar,
        content -> Text,
    }
}
